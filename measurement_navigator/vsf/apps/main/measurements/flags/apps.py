"""
    This app is meant to control the logic used to flag some
    measurement or set of measurements properly
"""

from django.apps import AppConfig


class FlagsConfig(AppConfig):
    name = "apps.main.measurements.flags"
